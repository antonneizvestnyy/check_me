#утилита grep

#ключи

- i -- игнорировать регистр

- e -- позволяет указать несколько паттернов

- r -- позволяет искать во всех файлов рекурсивно

- n -- показывает строку где указан паттерн

- w -- показыавет полное совпадение

- vi -- поиск по выводу предыдущей команды (если используется регулярное выражение)


#example

grep -n "main" <NAME>.py
dpkg -l | grep -i <NAME>
grep -r "<NAME>" /var/


#утилита zgrep
-- поиск в архивах gzip

#example

zgrep -i error /var/log/<NAME>.gz

#утилита find
# . -- относительный путь 

- name -- учитывается регистр в поиске имени файла
- iname -- не учитывать регистр в поиске имени файла

#example

find . -name *.py | grep -i <NAME>


